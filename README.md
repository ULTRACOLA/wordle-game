# 字謎報 Wordle

一個繁體中文介面的 Wordle 風格每日猜字遊戲，使用原生 HTML、CSS 與 JavaScript 製作，無需建置工具。

## 本機遊玩

直接用瀏覽器開啟 `index.html` 即可。

## GitHub Pages

此專案包含 GitHub Actions 設定。將內容推送到 GitHub repository 後，在 repository 的 Settings > Pages 將來源選為 GitHub Actions，之後每次推送到 `main` 都會自動部署。
